# Phase 1.2: Vendor Dashboard Initial CRUD - Plan

## Wave 0: Infrastructure & Tools
### Task 01.2-01-01: Install Form & Validation Libraries
- `npm install react-hook-form @hookform/resolvers zod sonner`
- `npx shadcn add table form input textarea select dialog toast`

## Wave 1: Dashboard Navigation & Shell
### Task 01.2-11-01: Implement Vendor Sidebar & Header
- Create `apps/web/src/components/modules/vendor/Sidebar.tsx`.
- Update `apps/web/src/app/(vendor)/layout.tsx` to include the sidebar and a simple vendor-focused header.

### Task 01.2-11-02: Build Dashboard Home Page
- Update `apps/web/src/app/(vendor)/page.tsx` with summary cards (Total Products, Total Sales placeholder, etc.).

## Wave 2: Inventory Management (Read)
### Task 01.2-21-01: Develop Vendor-Specific Fetching Hooks
- Create `useVendorProducts` hook in `apps/web/src/modules/products/hooks/useVendorProducts.ts`.
- Ensure it uses the correct endpoint: `GET /api/v1/products/vendor/{vendorId}`.

### Task 01.2-21-02: Implement Inventory Table
- Build the product listing table in `apps/web/src/app/(vendor)/inventory/page.tsx`.
- Include columns for Image, Name, Price, Category, and Actions.

## Wave 3: Product CRUD (Write)
### Task 01.2-31-01: Create Product Form Component
- Build `ProductForm.tsx` using `react-hook-form` and Zod schema.
- Support fields: Name, Description, Price, Category, Image URL.

### Task 01.2-31-02: Implement Create/Update/Delete Mutations
- Add `useCreateProduct`, `useUpdateProduct`, and `useDeleteProduct` hooks.
- Integrate with the UI via Dialogs and Toast notifications.

## Wave 4: Refinement & UX
### Task 01.2-41-01: Add Skeletons & Empty States
- Implement `DataTableSkeleton` for the inventory table.
- Handle "No products found" state with a CTA to add the first product.

### Task 01.2-41-02: Final Polish & Theme Sync
- Ensure the dashboard looks perfect in both light and dark modes.
- Verify consistent spacing (XL scale) and typography.
