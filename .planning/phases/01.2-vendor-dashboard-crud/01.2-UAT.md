# Phase 1.2 UAT: Vendor Dashboard Initial CRUD

## Session Information
- **Phase**: 1.2 - Vendor Dashboard Initial CRUD
- **Verifier**: Antigravity (AI Agent)
- **Environment**: Local Development (`apps/web`)

## Test Cases

### 1. Dashboard Layout & Navigation
expected: |
  Navigate to `http://localhost:3000/vendor`.
  - Sidebar is visible and correctly styled.
  - "Vendor Hub" branding is present.
  - Sidebar can be collapsed and expanded.
  - Navigation links (Dashboard, Inventory) are functional.
result: [pending]

### 2. Inventory Listing & Skeletons
expected: |
  Navigate to `http://localhost:3000/vendor/inventory`.
  - Table headers (Image, Name, Price, etc.) are visible.
  - Skeleton loaders appear while data is "fetching" (or until error state).
  - Empty state ("No products found") appears if no data is returned.
result: [pending]

### 3. Product Creation Dialog
expected: |
  Click "Add Product" button.
  - Modal opens with the Product Form.
  - Form contains Name, Price, Category, Description, and Image URL fields.
  - Close button and "Create Product" button are functional.
result: [pending]

### 4. Form Validation
expected: |
  Try to submit the "Add Product" form with empty fields.
  - Validation messages appear (e.g., "Name must be at least 3 characters").
  - Form prevents submission until errors are fixed.
result: [pending]

### 5. Theme Sync (Light/Dark)
expected: |
  Toggle the theme using the Moon/Sun icon in the dashboard header.
  - Sidebar, Header, and Table colors update correctly.
  - No visual artifacts or unreadable text in either mode.
result: [pending]

## Gaps

[none yet]
