---
phase: 1.1
slug: nextjs-storefront-foundation
status: planning
autonomous: true
waves:
  - 0: Setup
  - 1: Visual Foundation
  - 2: Data & Integration
  - 3: Pages
  - 4: Polish
created: 2026-05-02
---

# Phase 1.1: Next.js Storefront Foundation - Execution Plan

## Goal
Establish the fundamental Next.js App Router storefront with a responsive masonry grid, SEO-optimized PDPs, and pure TanStack Query fetching.

---

## Wave 0: Setup

### Task 01.1-01-01: Install Dependencies
<read_first>
- `apps/web/package.json`
</read_first>

<action>
Install the following dependencies in `apps/web`:
- `@tanstack/react-query`
- `@tanstack/react-query-devtools`
- `react-masonry-css`
- `lucide-react`
</action>

<acceptance_criteria>
- `apps/web/package.json` contains dependencies.
- `npm install` runs successfully in `apps/web`.
</acceptance_criteria>

### Task 01.1-01-02: Configure TanStack Query
<read_first>
- `apps/web/src/app/layout.tsx`
</read_first>

<action>
- Create `apps/web/src/core/providers/query-provider.tsx` (Client Component).
- Initialize `QueryClient` and wrap `children` in `QueryClientProvider`.
- Inject `QueryProvider` into `apps/web/src/app/layout.tsx`.
</action>

<acceptance_criteria>
- `QueryProvider` wraps the app layout.
- App runs without TanStack Query context errors.
</acceptance_criteria>

---

## Wave 1: Visual Foundation

### Task 01.1-11-01: Implement Responsive Masonry Grid
<read_first>
- `.planning/phases/01.1-nextjs-storefront-foundation/01.1-UI-SPEC.md`
</read_first>

<action>
- Create `apps/web/src/components/ui/MasonryGrid.tsx` using `react-masonry-css`.
- Apply spacing scale (XL - 32px gutter) from UI-SPEC.
- Configure responsive breakpoints (1 col mobile, 2 col tablet, 3-4 col desktop).
</action>

<acceptance_criteria>
- `MasonryGrid` component exists and is responsive.
</acceptance_criteria>

### Task 01.1-11-02: Create Product Card Component
<read_first>
- `.planning/phases/01.1-nextjs-storefront-foundation/01.1-UI-SPEC.md`
- `apps/web/src/app/globals.css`
</read_first>

<action>
- Create `apps/web/src/components/modules/storefront/ProductCard.tsx`.
- Use `Dominant/Secondary` colors and `Accent` for the "Add to Cart" CTA.
- Support Light/Dark modes using `oklch` tokens.
- Add smooth hover scale/shadow transitions.
</action>

<acceptance_criteria>
- `ProductCard` implements UI-SPEC typography and colors.
- Hover effects are visible.
</acceptance_criteria>

---

## Wave 2: Data & Integration

### Task 01.1-21-01: Develop Product Fetching Hooks
<read_first>
- `.planning/phases/01.1-nextjs-storefront-foundation/01.1-RESEARCH.md`
</read_first>

<action>
- Create `apps/web/src/modules/products/hooks/useProducts.ts`.
- Use `useQuery` to fetch from `http://localhost:8000/api/v1/products` (assuming gateway port).
- Implement basic error and loading state logic.
</action>

<acceptance_criteria>
- `useProducts` hook successfully fetches data from the backend.
</acceptance_criteria>

---

## Wave 3: Pages

### Task 01.1-31-01: Assemble Storefront Discovery Page
<read_first>
- `apps/web/src/app/(storefront)/page.tsx`
</read_first>

<action>
- Update `apps/web/src/app/(storefront)/page.tsx`.
- Integrate `MasonryGrid` with `useProducts` data.
- Handle empty/loading/error states using Copywriting Contract from UI-SPEC.
</action>

<acceptance_criteria>
- Main storefront page displays product grid from API.
- Empty state matches UI-SPEC.
</acceptance_criteria>

### Task 01.1-31-02: Implement SEO-optimized PDP
<read_first>
- `.planning/phases/01.1-nextjs-storefront-foundation/01.1-CONTEXT.md`
</read_first>

<action>
- Create `apps/web/src/app/(storefront)/product/[id]/page.tsx`.
- Fetch product details via `useProduct(id)` hook.
- Implement responsive layout with basic metadata (title/description).
</action>

<acceptance_criteria>
- PDP loads product data by ID.
- Page title matches product name.
</acceptance_criteria>

---

## Wave 4: Polish

### Task 01.1-41-01: Theme Toggle Implementation
<read_first>
- `apps/web/src/app/globals.css`
</read_first>

<action>
- Create a simple `ThemeToggle` component to switch between `light` and `dark` classes on `<html>`.
- Ensure it persists choice in `localStorage`.
</action>

<acceptance_criteria>
- Clicking toggle switches themes and persists on reload.
</acceptance_criteria>

---

## Verification Criteria

### Must Haves (Blocking)
- [ ] Product discovery page loads products from backend.
- [ ] Masonry grid is responsive across 3 breakpoints.
- [ ] Light/Dark mode transitions work for cards and background.
- [ ] PDP displays basic metadata correctly.

### Secure Behavior
- N/A for this foundation phase.

### Performance
- CLS < 0.1 for the Masonry grid.

---
*Phase: 1.1-Next.js Storefront Foundation*
*Plan date: 2026-05-02*
