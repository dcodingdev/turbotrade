# PLAN: Phase 3.2 — Advanced Catalog Management (Wave 1: Selection & Backend)

## Wave 1: Infrastructure & Bulk API
Establish the reusable selection hook and the backend endpoint for CSV generation.

### Tasks
<task id="3.2-01" type="execute" autonomous="true">
  <read_first>
    - apps/web/src/app/(vendor)/vendor/inventory/page.tsx
    - apps/web/src/modules/products/hooks/useVendorProducts.ts
  </read_first>
  <action>
    Create a reusable `useSelection` hook in `apps/web/src/hooks/useSelection.ts` that manages a set of selected IDs.
    - Export `selectedIds`, `toggle`, `selectAll`, `clear`, `isSelected`.
    - Integrated with generic ID arrays.
  </action>
  <acceptance_criteria>
    - `apps/web/src/hooks/useSelection.ts` exists and exports the required functions.
    - Unit test or manual verification shows selection state updates correctly.
  </acceptance_criteria>
</task>

<task id="3.2-02" type="execute" autonomous="true">
  <read_first>
    - services/product-service/src/modules/products/product.routes.ts
    - services/product-service/src/modules/products/product.controller.ts
  </read_first>
  <action>
    Implement `GET /api/v1/products/export` in `product-service`.
    - Supports `vendorId` and optional `ids` query parameter (array of IDs).
    - Fetches full product data (including categories and all images).
    - Flattens the data into CSV format:
      - `mainImage;subImages` semicolon-delimited for images.
      - Comma-delimited categories.
    - Sets `Content-Type: text/csv` and `Content-Disposition`.
  </action>
  <acceptance_criteria>
    - `services/product-service/src/modules/products/product.routes.ts` registers the `/export` route.
    - `services/product-service/src/modules/products/product.controller.ts` contains `exportProducts` logic.
    - A GET request to `/api/v1/products/export?vendorId=v_123` returns a valid CSV file.
  </acceptance_criteria>
</task>

## Verification
- [ ] `useSelection` hook correctly tracks row selection in a test component.
- [ ] `GET /api/v1/products/export` returns a CSV with correct headers and flattened image/category data.
