# SUMMARY: Phase 3.2-03 — Export UI (Wave 3)

## Objective
Wire up the selection logic and export triggers in the Vendor Dashboard.

## Changes
- **Web**: Integrated `useSelection` into `InventoryPage`.
- **Web**: Added a Checkbox column and a "Select All" header checkbox to the product table.
- **Web**: Added "Export All" button in the page header.
- **Web**: Implemented a sticky "Selection Action Bar" that appears when products are checked.
- **Web**: Implemented `handleExport` with dynamic query parameter construction for bulk downloads.

## Verification Results
- Selection state is correctly tracked and visual feedback (row highlighting) works.
- "Export Selected" correctly triggers the backend download with filtered IDs.
- Sticky bar animates in/out as expected.
