# PLAN: Phase 3.2 — Advanced Catalog Management (Wave 2: Media Uploads)

## Wave 2: Interactive Media Ingestion
Replace the static URL input with a hybrid Drag-and-Drop + URL component.

### Tasks
<task id="3.2-03" type="execute" autonomous="true">
  <read_first>
    - apps/web/src/modules/products/components/ProductForm.tsx
    - apps/web/src/components/ui/input.tsx
  </read_first>
  <action>
    Create `ImageUpload` component in `apps/web/src/modules/products/components/ImageUpload.tsx`.
    - Uses `react-dropzone` (install if missing).
    - Supports immediate upload to the backend on drop.
    - Shows a progress state and then a thumbnail preview of the uploaded image.
    - Provides a way to clear the image.
  </action>
  <acceptance_criteria>
    - `ImageUpload.tsx` exists and renders a dropzone.
    - Successfully uploads a file and displays the thumbnail.
  </acceptance_criteria>
</task>

<task id="3.2-04" type="execute" autonomous="true">
  <read_first>
    - apps/web/src/modules/products/components/ProductForm.tsx
    - apps/web/src/modules/products/components/ImageUpload.tsx
  </read_first>
  <action>
    Integrate `ImageUpload` into `ProductForm.tsx`.
    - Keep the existing `imageUrl` input as a secondary field.
    - When an image is uploaded, populate the `imageUrl` field (or a hidden field) so the form submission works as before.
    - Ensure `initialData` correctly populates the preview.
  </action>
  <acceptance_criteria>
    - `ProductForm.tsx` shows the new dropzone.
    - Submitting the form with a dropped image successfully creates/updates the product.
    - Submitting with just a URL still works.
  </acceptance_criteria>
</task>

## Verification
- [ ] Drop an image into `ProductForm`, verify it uploads immediately and shows a preview.
- [ ] Save the product and confirm the URL in the database matches the uploaded file.
