# PLAN: Phase 3.2 — Advanced Catalog Management (Wave 3: Export UI)

## Wave 3: Bulk Action Integration
Wire up the selection hook and export buttons to the inventory table.

### Tasks
<task id="3.2-05" type="execute" autonomous="true">
  <read_first>
    - apps/web/src/app/(vendor)/vendor/inventory/page.tsx
    - apps/web/src/hooks/useSelection.ts
  </read_first>
  <action>
    Integrate `useSelection` into `InventoryPage.tsx`.
    - Add a checkbox column to the `Table`.
    - Add "Select All" checkbox in the `TableHeader`.
    - Maintain selection state across the product list.
  </action>
  <acceptance_criteria>
    - Inventory table has functional checkboxes for each row.
    - "Select All" correctly toggles all visible rows.
  </acceptance_criteria>
</task>

<task id="3.2-06" type="execute" autonomous="true">
  <read_first>
    - apps/web/src/app/(vendor)/vendor/inventory/page.tsx
    - services/product-service/src/modules/products/product.controller.ts
  </read_first>
  <action>
    Add Export buttons to `InventoryPage.tsx`.
    - **Header Button**: "Export All CSV" next to "Add Product". Triggers download for all vendor products.
    - **Selection Bar**: When rows are selected, show a floating bar or a button "Export Selected ({n})" that triggers download only for those IDs.
    - Implementation: Use `window.location.href` or a hidden link to trigger the backend CSV download.
  </action>
  <acceptance_criteria>
    - "Export All CSV" button exists and works.
    - "Export Selected" appears only when rows are checked and works correctly.
  </acceptance_criteria>
</task>

## Verification
- [ ] Select 2 products, click "Export Selected", verify the CSV contains only those 2 rows.
- [ ] Click "Export All CSV", verify the CSV contains all vendor products.
