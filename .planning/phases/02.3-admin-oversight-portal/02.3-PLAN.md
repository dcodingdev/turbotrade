---
wave: 1
depends_on: [2.1]
files_modified:
  - services/auth-service/src/modules/users/user.model.ts
  - services/auth-service/src/modules/users/user.controller.ts
  - services/auth-service/src/modules/auth/auth.controller.ts
  - services/order-service/src/modules/order.controller.ts
  - services/order-service/src/modules/order.routes.ts
  - apps/web/src/app/(admin)/layout.tsx
  - apps/web/src/app/(admin)/page.tsx
  - apps/web/src/app/(admin)/users/page.tsx
  - apps/web/src/app/(admin)/products/page.tsx
autonomous: true
---

# Phase 2.3: Admin Oversight Portal - Execution Plan

## Tasks

<task id="1" title="Backend: User Management & Suspension (Auth Service)">
<read_first>
- services/auth-service/src/modules/users/user.model.ts
- services/auth-service/src/modules/users/user.controller.ts
- services/auth-service/src/modules/auth/auth.controller.ts
</read_first>
<action>
1. Update `User` model to include `isSuspended: { type: Boolean, default: false }`.
2. Implement `getAllUsers` in `user.controller.ts` (Paginated, Admin only).
3. Implement `toggleUserSuspension` in `user.controller.ts`:
   - Checks if user exists and is not the current admin.
   - Toggles the `isSuspended` flag.
4. Update `auth.controller.ts` (Login):
   - Reject login attempts if `user.isSuspended === true` with a clear message: "Your account has been suspended."
5. Register routes in `user.routes.ts`.
</action>
<acceptance_criteria>
- `GET /api/v1/users` returns all users to admins.
- `PATCH /api/v1/users/:id/suspend` successfully toggles state.
- Suspended users receive an error during login.
</acceptance_criteria>
</task>

<task id="2" title="Backend: Global Order Feed (Order Service)">
<read_first>
- services/order-service/src/modules/order.controller.ts
- services/order-service/src/modules/order.routes.ts
</read_first>
<action>
1. Implement `getAllOrders` in `order.controller.ts`:
   - Paginated retrieval of all platform orders.
2. Update `order.routes.ts`:
   - Uncomment or add `router.get("/all", authorize([UserRole.ADMIN]), orderController.getAllOrders)`.
</action>
<acceptance_criteria>
- `GET /api/v1/orders/all` returns paginated orders from all customers and vendors.
</acceptance_criteria>
</task>

<task id="3" title="Frontend: Admin Sidebar & Dashboard Stats">
<read_first>
- apps/web/src/app/(admin)/layout.tsx
- apps/web/src/components/ui/card.tsx
</read_first>
<action>
1. Update `apps/web/src/app/(admin)/layout.tsx` to include a persistent Sidebar with links to: Dashboard, Users, Products, Orders.
2. Implement `apps/web/src/app/(admin)/page.tsx` (Dashboard):
   - Display summary cards using `Card` component for:
     - Total Registered Users.
     - Total Platform Products.
     - Total Platform Sales (USD).
</action>
<acceptance_criteria>
- Admin layout has a functional sidebar.
- Dashboard displays high-level platform metrics.
</acceptance_criteria>
</task>

<task id="4" title="Frontend: User & Product Management Dashboards">
<read_first>
- apps/web/src/components/ui/table.tsx
</read_first>
<action>
1. Create `apps/web/src/app/(admin)/users/page.tsx`:
   - Searchable table of all users.
   - Display Role badges.
   - Add a "Suspend/Unsuspend" button that calls the backend.
2. Create `apps/web/src/app/(admin)/products/page.tsx`:
   - List of all platform products.
   - Ability to "Unpublish" (toggle isDraft) any product.
</action>
<acceptance_criteria>
- Admin can search for users and toggle their suspension status.
- Admin can view any product and moderate its visibility.
</acceptance_criteria>
</task>

## Verification
- Log in as Admin and verify sidebar navigation.
- Suspend a test user and verify they cannot log in.
- Unpublish a product as Admin and verify it no longer appears in the storefront.
- Verify that global metrics match the aggregate data in the database.
